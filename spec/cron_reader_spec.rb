require 'spec_helper'

describe CronReader do
  it 'has a version number' do
    expect(CronReader::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
